# --- Core (required) ---
DATABASE_URL="postgresql://user:password@host:5432/horoprive?schema=public"
AUTH_SECRET="generate-with: openssl rand -base64 32"
AUTH_URL="http://localhost:3000"

# --- Email (magic-link sign-in + notifications) ---
EMAIL_SERVER="smtp://user:pass@smtp.example.com:587"
EMAIL_FROM="HORO PRIVÉ <advisors@horoprive.example>"

# --- Object storage for the documents vault (S3 / Cloudflare R2) ---
STORAGE_BUCKET=""
STORAGE_REGION=""
STORAGE_ACCESS_KEY_ID=""
STORAGE_SECRET_ACCESS_KEY=""
STORAGE_ENDPOINT=""

# --- Market data / FX (valuations + landed-cost calculator) ---
MARKET_DATA_API_KEY=""
FX_API_KEY=""

# --- Later phases (leave blank until configured) ---
STRIPE_SECRET_KEY=""
WHATSAPP_TOKEN=""
CALENDAR_API_KEY=""
